{
  "buildCommand": "npm run deploy:admin",
  "outputDirectory": "dist-admin/client",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
